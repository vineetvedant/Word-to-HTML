{
  "name": "word-to-html",
  "version": "1.0.0",
  "description": "Static Word to HTML 1.0 converter site",
  "private": true,
  "scripts": {
    "postinstall": "node -e \"require('node:fs').rmSync('.wrangler/deploy/config.json',{force:true})\"",
    "deploy": "wrangler deploy",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "vercel": "^57.0.0",
    "wrangler": "4.114.0"
  }
}
